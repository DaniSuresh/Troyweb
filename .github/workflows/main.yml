on: push
name: Troyweb
jobs:
  artifactoryUpload:
    name: Upload Troyweb
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1.2.0
  
      with:
          repository: 'https://daniainavilli.jfrog.io/ui/repos/tree/General/local' 
          ref: '"${{ secrets.JFROG_USER_NAME }}" , $JfrogPassword'
          token: ${{ secrets.RT_ACCESSTOKEN }}
           path:"D:\a\Troyweb\Troyweb\Troyweb"
         
 
